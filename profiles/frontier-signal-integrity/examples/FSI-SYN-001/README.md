# FSI-SYN-001 — Synthetic Contested Narrative

This fully synthetic adversarial example demonstrates fail-closed handling of a repeated frontier claim whose apparent corroboration resolves to one source root. No real person, organization, program, or private source is represented.

Expected result: `FSI_REVIEW_REQUIRED` with recommendation `REQUIRE_CORROBORATION`. The case intentionally preserves three blocking conditions: upstream Perception Integrity review required (`FSI-01`), independence overstated / insufficient independent roots (`FSI-03`), and missing bridge evidence for a supported-anchor → unsupported-extension reasoning pattern (`FSI-04`).

The result is an integrity finding, not a truth determination. Human review remains required.
